{
  "name": "@sonate/orchestrate",
  "version": "1.0.0",
  "description": "Production agent management and orchestration for SONATE platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "test": "npm run build && c8 --check-coverage --branches 40 --functions 40 --lines 40 --statements 40 node dist/tests/run-tests.js"
  },
  "keywords": [
    "sonate",
    "orchestration",
    "agents",
    "did",
    "vc",
    "kubernetes"
  ],
  "author": "YSEEKU",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-kms": "^3.954.0",
    "@digitalbazaar/did-method-key": "^5.0.0",
    "@google-cloud/kms": "^5.2.1",
    "@sonate/core": "file:../core",
    "@sonate/persistence": "file:../persistence",
    "bcrypt": "^5.1.1",
    "ioredis": "^5.8.2",
    "node-vault": "^0.10.9"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
