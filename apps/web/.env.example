# YSEEKU SONATE Web App — Environment Variables
# Copy to .env.local for local development

# ─── Required ────────────────────────────────────────────────────────
# Backend API URL (used by Next.js rewrites + client-side Socket.IO)
NEXT_PUBLIC_BACKEND_URL=http://localhost:3001

# ─── Recommended ─────────────────────────────────────────────────────
# Server-side URL for API route handlers (falls back to NEXT_PUBLIC_BACKEND_URL)
# On Vercel, set this to the same backend URL for server-to-server calls
# INTERNAL_API_URL=https://yseeku-backend.fly.dev

# JWT secret for middleware auth verification (if not set, middleware calls backend /api/auth/me)
# JWT_SECRET=your-jwt-secret-here

# ─── Optional ────────────────────────────────────────────────────────
# Public app URL (used for CORS validation in security utils)
# NEXT_PUBLIC_APP_URL=https://your-vercel-domain.vercel.app

# WebSocket URL override (defaults to NEXT_PUBLIC_BACKEND_URL)
# NEXT_PUBLIC_WS_URL=wss://yseeku-backend.fly.dev

# API URL override for policy dashboard metrics (defaults to same-origin)
# NEXT_PUBLIC_API_URL=

# CSRF secret (falls back to JWT_SECRET)
# CSRF_SECRET=
