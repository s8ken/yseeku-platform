# Nixpacks configuration for Railway
[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["cd ../.. && npm install"]

[phases.build]
cmds = ["cd ../.. && npm run build:backend"]

[start]
cmd = "node dist/index.js"

[variables]
NODE_ENV = "production"
