/**
 * Policy Alerts WebSocket Routes
 * 
 * NOTE: This file is disabled - @sonate/policy packages not yet available
 * Routes will be restored when packages are ready for production
 */

import { Router } from 'express';

const router = Router();

// Routes disabled - packages not available
// TODO: Restore when @sonate/policy and @sonate/monitoring packages are built

export default router;
